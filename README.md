# Integração CloudFlare <-> Clouds

Por enquanto, apenas com a OCI.

```shell
# Se estiver usando venv
python -m venv .venv/
source .venv/bin/activate

pip install -r requirements.txt
python main.py
```
